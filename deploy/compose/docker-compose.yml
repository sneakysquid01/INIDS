version: "3.9"
services:
  inids-web:
    image: python:3.10-slim
    working_dir: /app
    command: >
      sh -lc "pip install --no-cache-dir -r requirements.txt && python -m web_app.app"
    ports:
      - "5000:5000"
    volumes:
      - ../../:/app
    env_file:
      - ../../.env.example
