// Baseline scenario — idle only, no attacks
// DemoController runs the idle ticker regardless; this is a no-op timeline.
export const baseline = {
    id: 'baseline',
    name: 'Idle',
    duration: 0,
    timeline: [],
};
